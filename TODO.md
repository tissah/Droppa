# Task: Match login + create account UI to Resources/Images/Droppa3

- [ ] Update `Views/LoginPage.xaml` to use `Droppa3.png` branding and align layout with the Droppa3 interface (gradient background + rounded card).
- [ ] Update `Views/RegisterPage.xaml` to use the same gradient + rounded card interface and `Droppa3.png` branding.
- [ ] Ensure all existing bindings/commands remain unchanged.
- [x] Build the project to ensure XAML compiles.

